<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.aditya.vendicinefinal">

    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Intro">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".DeviceList"
            android:theme="@style/Base.Theme.AppCompat.Dialog.FixedSize" />
        <activity android:name=".SelectionUI" />
        <activity android:name=".tabletSelection" />
        <activity
            android:name=".PopupQuantity"
            android:theme="@style/NoTitleDialog" />
        <activity
            android:name=".otherSelection"
            android:theme="@style/NoTitleDialog" />
        <activity android:name=".moneyDeal" />
        <activity android:name=".lastActivity" />
        <activity android:name=".dummyIntro" />
        <activity
            android:name=".PopupActivity"
            android:theme="@style/NoTitleDialog" />
        <activity android:name=".nutriSel" />
        <activity android:name=".wipesSel"></activity>
    </application>

</manifest>